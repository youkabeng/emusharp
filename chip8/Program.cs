using System;

namespace EmuSharp.Chip8
{
    class Program
    {
        static void Main()
        {
            Console.WriteLine("Hello from chip8");
            // Chip8 c = new Chip8();
            // setup render system and register input callbacks
            // setupGraphics()
            // setupInput()

            // init chip8 system and load the game into the memory
            // chip8.init()
            // chip8.load("game")

            // Emulation loop
            while (true)
            {
                // chip8.tick()

                // if the draw flag is set, update the screen
                // if(chip8.drawFlag)
                //      drawGraphics();

                // store key press state (Press and Release)
                // chip8.setKeys();
                break;
            }
        }
    }
}